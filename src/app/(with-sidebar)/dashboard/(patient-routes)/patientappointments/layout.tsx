export const metadata = {
    title: 'Doctorii | Patient Appointments'
};

const PatientAppointmentsLayout = ({children}: {children: React.ReactNode}) => {
  return children;
}

export default PatientAppointmentsLayout;