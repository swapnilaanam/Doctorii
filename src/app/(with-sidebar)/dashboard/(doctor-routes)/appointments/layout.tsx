export const metadata = {
    title: 'Doctorii | Doctor Appointment'
}

const DoctorAppointmentLayout = ({children} : {children: React.ReactNode}) => {
  return children;
}

export default DoctorAppointmentLayout;