export const metadata = {
    title: 'Doctorii | Doctor Schedules'
}

const DoctorScheduleLayout = ({children} : {children: React.ReactNode}) => {
  return children;
}

export default DoctorScheduleLayout;