export const metadata = {
    title: 'Doctorii | Book Appointment'
};

const BookAppointmentLayout = ({children}: {children: React.ReactNode}) => {
  return children;
}

export default BookAppointmentLayout;