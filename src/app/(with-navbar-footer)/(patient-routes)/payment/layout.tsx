export const metadata = {
    title: 'Doctorii | Appointment Payment'
};

const AppointmentPaymentLayout = ({children}: {children: React.ReactNode}) => {
  return children;
}

export default AppointmentPaymentLayout;