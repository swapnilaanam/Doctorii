export const metadata = {
    title: 'Doctorii | Book Ticket'
};

const DiagnosisPaymentLayout = ({children}: {children: React.ReactNode}) => {
  return children;
}

export default DiagnosisPaymentLayout;