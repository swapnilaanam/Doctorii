export const metadata = {
    title: 'Doctorii | BMI Calculator'
};

const BMICalculatorLayout = ({children}: {children: React.ReactNode}) => {
  return children;
}

export default BMICalculatorLayout;